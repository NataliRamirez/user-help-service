spring:
  application:
    name: user-help-service
  cloud:
    consul:
      enabled: true
      host: localhost
      port: 8500
      discovery:
        service-name: user-help-service
        health-check-path: /actuator/health
        health-check-interval: 10s

server:
  port: ${SERVER_PORT:8085}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always

qvenly:
  auth:
    user-id-header: X-User-Id
    user-email-header: X-User-Email
    user-role-header: X-Rol
  help:
    support-persistence: in-memory
    blocked-words:
      - politica
      - religión
      - religion
      - apuestas
      - drogas
      - sexo
      - violencia
      - armas
      - hacking
      - malware
      - medicina
      - finanzas
      - inversión
      - inversion
      - criptomonedas
    allowed-keywords:
      - qvenly
      - evento
      - eventos
      - usuario
      - usuarios
      - rol
      - roles
      - perfil
      - contraseña
      - password
      - login
      - registro
      - plan
      - planes
      - facturación
      - facturacion
      - actividad
      - actividades
      - notificación
      - notificaciones
      - invitación
      - invitaciones
      - participante
      - participantes
      - asistente
      - asistentes
      - organizador
      - administrador
      - admin
      - jurado
      - soporte
      - ayuda
      - manual
      - chatbot
      - solicitud
      - solicitudes
      - centro de ayuda
  gemini:
    api-key: ${GEMINI_API_KEY:}
    base-url: ${GEMINI_BASE_URL:https://generativelanguage.googleapis.com/v1beta}
    model: ${GEMINI_MODEL:gemini-1.5-flash}
    timeout-seconds: ${GEMINI_TIMEOUT_SECONDS:20}
